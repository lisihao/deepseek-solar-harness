# AGENTS.md — GitHub Actions

CI and release jobs use the supported Linux and macOS paths. Do not add Windows runners, Wine, PowerShell, or Windows-specific gates; keep this guidance aligned with the workflow definitions.
