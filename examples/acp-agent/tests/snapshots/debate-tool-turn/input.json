{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Use the debate tool to compare the two fixture options, then return the full deterministic debate transcript." }
  ]
}
