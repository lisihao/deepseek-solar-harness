# Transcript Fixtures

This directory contains fixture-backed transcripts for the active Layer 2
behavior check and controlled replay samples.

- `with-aegis/` holds positive transcripts that should satisfy the scenario behavior contracts
- `tests/e2e/baselines/without-aegis/` holds contrast transcripts that intentionally lack the same level of method-pack behavior

These fixtures are advisory verification inputs, not authoritative runtime outputs.
