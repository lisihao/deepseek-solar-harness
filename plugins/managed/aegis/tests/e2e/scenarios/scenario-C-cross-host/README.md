# Scenario C: Cross Host

Goal:

- compare the same planning-oriented request across at least two hosts

Current bootstrap role:

- define the comparison prompt
- define expected core behavior invariants
- mark the scenario as environment-dependent
