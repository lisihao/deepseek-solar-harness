# Webhook Console

Failed deliveries can currently be retried one at a time. The repository does
not define a bulk-selection limit, authorization rules, idempotency behavior,
or whether retries should be exposed through the API as well as the console.
