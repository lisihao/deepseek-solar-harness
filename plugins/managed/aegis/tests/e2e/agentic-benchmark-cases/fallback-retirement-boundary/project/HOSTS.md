# Host inventory

The managed Linux hosts use `socket_path`. One third-party appliance loads the
same configuration format, but its deployed keys and upgrade window are not
available in this repository.
