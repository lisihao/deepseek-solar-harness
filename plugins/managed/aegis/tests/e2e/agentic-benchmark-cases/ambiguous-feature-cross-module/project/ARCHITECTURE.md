# Account surfaces

Identity owns account records, Billing owns invoices, and Audit owns immutable
events. The repository has no decision for merge authority, conflict handling,
invoice ownership, reversibility, or audit representation.
