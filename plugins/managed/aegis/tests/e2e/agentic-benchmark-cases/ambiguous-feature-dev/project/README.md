# Activity Board

The activity board currently supports an on-screen list and a JSON API. The
repository does not define which activity fields are public, how date ranges
are selected, or whether an export is synchronous.
