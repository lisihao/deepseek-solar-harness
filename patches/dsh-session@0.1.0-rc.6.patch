diff --git a/lib/index.js b/lib/index.js
index ac18f2b..bf005d2 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1438,8 +1438,12 @@ class Session {
 	*   append reentered while this acceptance/publication boundary is open also
 	*   rejects before the log changes.
 	*/
 	append(type, data, ...opts) {
-		const surfaceOpts = opts[0];
+		const appendOpts = opts[0];
+		const surfaceOpts = appendOpts;
+		const ignorable = isSurfaceEligibleType(type)
+			? void 0
+			: appendOpts?.ignorable;
 		const surfaceMetadata = {
 			...surfaceOpts?.sourceEventSeqs === void 0 ? {} : { sourceEventSeqs: surfaceOpts.sourceEventSeqs },
 			...surfaceOpts?.surfaceOp === void 0 ? {} : { surfaceOp: surfaceOpts.surfaceOp }
@@ -1455,7 +1459,8 @@ class Session {
 			type,
 			seq: this.log.length,
 			time: Date.now(),
 			data: dataSnapshot,
+			...ignorable === true ? { ignorable } : {},
 			...surfaceMetadataSnapshot
 		});
 		this.surfaceManager.validateNext(event);
diff --git a/lib/types/index.d.ts b/lib/types/index.d.ts
index 0b64d1d..b16dc3c 100644
--- a/lib/types/index.d.ts
+++ b/lib/types/index.d.ts
@@ -10,7 +10,7 @@ import type { Scoped } from '@deepseek-ai/dsh-scope';
 import type { Message } from '@deepseek-ai/dsh-llm';
 import { SessionId } from './types.ts';
 import type { TypertLookup } from '@deepseek-ai/dsh-typert-protocol';
-import type { CreateSessionOptions, EpochHeader, PrepareSessionOptions, RequestContext, SessionEvent, SessionEventMap, SessionEventType, SessionHeader, SurfaceIntent, SurfaceEventType } from './types.ts';
+import type { CreateSessionOptions, EpochHeader, LogIntent, PrepareSessionOptions, RequestContext, SessionEvent, SessionEventMap, SessionEventType, SessionHeader, SurfaceIntent, SurfaceEventType } from './types.ts';
 import type { SessionSurface } from './surface.ts';
 export * from './types.ts';
 export { SessionPreparation } from './preparation.ts';
@@ -209,7 +209,7 @@ export declare class Session {
      *   append reentered while this acceptance/publication boundary is open also
      *   rejects before the log changes.
      */
-    append<T extends SessionEventType>(type: T, data: SessionEventMap[T], ...opts: T extends SurfaceEventType ? [opts: SurfaceIntent] : []): SessionEvent<T>;
+    append<T extends SessionEventType>(type: T, data: SessionEventMap[T], ...opts: T extends SurfaceEventType ? [opts: SurfaceIntent] : [opts?: LogIntent]): SessionEvent<T>;
     /** Cached fold of the request-header events — see {@link requestHeader}. */
     private headerFold;
     /** Log position (events consumed) the header fold has reached. */
diff --git a/lib/types/types.d.ts b/lib/types/types.d.ts
index ed2f525..8bb34c5 100644
--- a/lib/types/types.d.ts
+++ b/lib/types/types.d.ts
@@ -407,6 +407,15 @@ export interface SurfaceIntent {
      */
     sourceEventSeqs?: number[];
 }
+/**
+ * Optional persistence metadata for a log-only {@link Session.append} call.
+ * `ignorable` permits a reader that does not know a plugin-owned event type to
+ * reconstruct the core session without that extension. Omit it when the event
+ * changes how the core log must be interpreted.
+ */
+export interface LogIntent {
+    ignorable?: true;
+}
 /**
  * One immutable entry in the session log.
  *
