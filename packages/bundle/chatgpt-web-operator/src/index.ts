/**
 * Marker plugin for the opt-in ChatGPT Web physical-operator bundle.
 *
 * The bundle's runtime composition is declared by `cordis.patch.yml`; this
 * module intentionally owns no provider implementation or browser transport.
 * @module @deepseek-ai/dsh-chatgpt-web-operator
 */

/** Stable marker used by package-level composition checks. */
export const name = 'chatgpt-web-operator-bundle'
